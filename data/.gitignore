# Ignore database files but keep the directory
*.db
*.db-wal
*.db-shm
*.lock
backups/*.db

# NDJSON dumps are imported from here and are hundreds of megabytes
*.ndjson
*.jsonl

# Keep these files
!.gitignore
!.htaccess
!backups/.htaccess
